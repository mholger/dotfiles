which vim >/dev/null
if [ $? -eq 0 ]
then
    alias vi=vim
fi

if [[ `uname -r` =~ "Microsoft" ]]
then
    alias hab=hab.exe
    alias choco=choco.exe
    alias nomad=nomad.exe
    alias consul=consul.exe
    alias vault=vault.exe
    alias packer=packer.exe
    alias terraform=terraform.exe
fi
